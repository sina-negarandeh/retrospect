# Development dependencies (includes all production deps)
-r requirements.txt

# Testing
pytest==8.3.5
pytest-asyncio==0.24.0
pytest-cov==6.1.0

deepeval>=1.4.0
ragas>=0.1.7
# Pin pyarrow to avoid conflicts with mlflow 3.1.0 and deepeval
pyarrow<21.0.0,>=4.0.0

# Linting & formatting
ruff==0.11.13
mypy==1.16.0
